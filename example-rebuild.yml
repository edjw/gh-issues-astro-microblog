# name: Redeploy on Issue Change

# on:
#   issues:
#     types: [opened, edited, closed, deleted, reopened, labeled, unlabeled]

#   issue_comment:
#     types: [created, edited, deleted]

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Redeploy
#         run: curl -X POST ${{ secrets.vercel_deploy_hook}}
