---
type Props = {
	date: string,
};
const { date } = Astro.props;

const d = new Date(date);
---

<time datetime={d.toISOString()}>
	{
		d.toLocaleDateString("en-gb", {
			year: "numeric",
			month: "short",
			day: "numeric",
			hour: "numeric",
			minute: "numeric",
		})
	}
</time>
